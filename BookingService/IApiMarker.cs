namespace BookingService;

/// <summary>
/// Marker type used by tests to locate this assembly's entry point.
/// Both services generate a global `Program` class from top-level statements,
/// so `WebApplicationFactory&lt;Program&gt;` would be ambiguous.
/// </summary>
public interface IApiMarker;
